/* import { AddBorderDirective } from './add-border.directive';

describe('AddBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBorderDirective();
    expect(directive).toBeTruthy();
  });
});
 */